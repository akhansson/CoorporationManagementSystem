<Window x:Class="CooperationApp.EditCompany"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CooperationApp"
        mc:Ignorable="d"
        Title="EditCompany" Height="100" Width="300"
        MaxHeight="100" MaxWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <Label Content="Company name: "
               Grid.Column="0"
               Grid.Row="0"/>
        <TextBox x:Name="changeCompanyNameTextBox"
                 VerticalAlignment="Top"
                 Margin="5"
                 Grid.Column="1"
                 Grid.Row="0"/>
        <Button x:Name="editCompanyNameButton"
                Content="Save"
                Grid.Column="1"
                Grid.Row="1"
                VerticalAlignment="Center"/>
    </Grid>
</Window>
